include mixins.pug

block vars

doctype html
html.page.no-js(lang="ru")
  +head()
  body
    +browsehappy()
    .page__inner
      .page__content
        block header
          +header(header)
        block content
      block footer
        .page__footer
          +footer()

    block scripts
      script(src="./assets/js/vendor.js")
      script(src="./assets/js/script.js")
